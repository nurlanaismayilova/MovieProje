@model List<MovieProject.Models.Movie>

<h2>Movies</h2>
<table class="table">
    <thead><tr><th>Title</th><th>Release Year</th></tr></thead>
    <tbody>
        @foreach (var m in Model)
        {
            <tr><td>@m.Title</td><td>@m.ReleaseYear</td></tr>
        }
    </tbody>
</table>
